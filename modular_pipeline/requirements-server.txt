# Runtime deps for the deployed study backend (server.py code path only —
# NOT the full vision pipeline, which runs in prep, not in deploy).
flask>=3.0
flask-cors>=4.0
openai>=1.0
python-dotenv>=1.0
python-docx>=1.0
