# Dev/test extras for the API service (TestClient needs httpx).
# Compile with:
#
#   uv pip compile services/api/requirements-dev.in -o services/api/requirements-dev.txt \
#     --python-version 3.12 --python-platform x86_64-unknown-linux-gnu
-r requirements.in
pytest>=8
httpx>=0.27
