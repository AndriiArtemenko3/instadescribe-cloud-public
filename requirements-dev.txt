# Dev + CI tooling. Light by design: the pure-logic tests don't need the ML stack
# (torch / faster-whisper / silero-vad), so CI stays fast. Install the full
# pipeline separately with `pip install -r requirements.txt` when running it end to end.
ruff>=0.6
pytest>=8.0
pytest-cov>=5.0

# Light runtime deps the tested modules import at module load.
openai>=1.0
python-dotenv>=1.0
flask>=3.0
flask-cors>=4.0
