# Loaded by `vite ... --mode demo` (see the `demo` script in package.json).
# No secrets here — safe to commit. This produces the zero-key, zero-backend
# build where every OpenAI / TTS / render call is served from committed fixtures
# (see src/lib/demoApi.ts).
VITE_DEMO_MODE=1
VITE_API_BASE=
